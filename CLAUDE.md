# CLAUDE.md — 精力助手（brain-gears）

## 这是什么

一个启发自《大脑效率革命》(Hyper-efficient) 的**零依赖**精力管理工具。核心概念是三挡位模型：

- 挡位1 · 慢速/充电（累、空转、该休息）
- 挡位2 · 心流/深度（能专注）
- 挡位3 · 冲刺/快速（赶时间、紧急交付）

它把「今天几点起床 + 睡了几小时」变成一张**今日精力地形图**（时间轴），自动把任务按精力节律排进去，并提供「检测我的档位」「我卡住了」两个即时动作。任务是 AI 读 md 正文后拆出来的，不是靠标题猜。

## 技术栈

- **纯 HTML/CSS/JS 单页应用，零 npm 依赖**，无框架、无构建步骤。
- 一个极简 **Node `http` 静态服务器**（`server.js`，只用内置模块），绑定 `127.0.0.1:8642`。必须有它的原因：自动读取 `inbox/tasks.json` 需要 localhost（`file://` 下浏览器禁止 fetch）。
- 持久化用浏览器 **localStorage**（有内存降级），key 前缀 `brain-gears:`。
- 主题用 **CSS 自定义属性**：浅色在 `:root`，深色在 `body.dark`（覆盖同名变量）。

## 文件结构（改哪个文件）

| 文件 | 职责 | 什么时候动它 |
|------|------|-------------|
| `index.html` | 页面 DOM 结构：顶部栏、卡片、左右栏、面板、弹窗 | 增删/重排卡片、面板、按钮、弹窗 |
| `styles.css` | **所有样式**：配色变量、网格布局、圆角、阴影、组件 | **改颜色、改主题、改间距/圆角/阴影** |
| `app.js` | **所有逻辑**：渲染、排程、挡位检测、导入、事件绑定 | 改行为、渲染内容、数据逻辑 |
| `server.js` | 本地静态服务器 | 基本不用动 |
| `启动.bat` | Windows 启动入口 | 基本不用动（见「坑」） |
| `inbox/tasks.json` | AI 任务输入（契约见 `tasks.schema.json`） | 由 skill 生成，勿手改 |
| `.claude/skills/decompose-tasks/SKILL.md` | 把 md 拆成任务的 skill | 改拆解规则 |
| `README.md` | 面向用户的说明 | 改用户文档 |

## 改配色 / 改主题 —— 只动 `styles.css`

所有颜色都在 `styles.css` 顶部两个变量块里，**改这里一处全局生效**：

- `:root { ... }` → 浅色主题
- `body.dark { ... }` → 深色主题（覆盖同名变量）

当前调色板（浅色）——「低饱和浅米白 · 青蓝×青绿薄荷」：

| 变量 | 值 | 用途 |
|------|-----|------|
| `--bg` | `#f8f6f1` | 页面背景（低饱和浅米白，透亮、少黄少灰） |
| `--surface` | `#ffffff` | 卡片纯白（比背景亮一阶） |
| `--surface-2` | `#f3f0ea` | 次级底（输入框、次级面板、标签底） |
| `--text` / `--text-2` / `--text-3` | `#3b352c` / `#6f675c` / `#a69e90` | 标题深棕灰 / 正文中灰 / 辅助浅灰（三级明度阶梯） |
| `--border` / `--line` | `#ece7dd` / `#f2eee6` | 极淡边框 / 分隔线 |
| `--accent` | `#3d7ba6` | **青蓝（工作/主按钮/当前段，全页唯一主强调色，冷静专注）** |
| `--accent-strong` / `--accent-soft` / `--on-accent` | `#2f5f86` / `#e4eef6` / `#ffffff` | 青蓝深色 / 浅底 / 青蓝底上的文字色 |
| `--gear-1` / `--gear-2` / `--gear-3` | `#3d8f7c` / `#3d7ba6` / `#c05b34` | 三挡位专属色：充电=青绿 / 心流=青蓝 / 冲刺=珊瑚 |
| `--charge` | `#3d8f7c` | 地形图「充电」= 青调深绿（白字） |
| `--rest` / `--rest-text` | `#d2e8da` / `#2f4a40` | 地形图「休息」= 淡薄荷绿（深字） |
| `--pill-bg` / `--pill-text` | `#f1ece3` / `#837a6c` | 挡位 pill / 任务类型 tag（暖中性） |
| `--stuck-text` | `#c05631` | 「我卡住了」按钮 = 暖珊瑚描边（不再用浅粉棕填充） |
| `--done` | `#3d8f7c` | 已完成 / 勾选 = 青绿 |
| `--toast-bg` / `--toast-text` | 按主题 | toast（浅色=深底浅字，深色=浅底深字，避免反转刺眼） |
| `--shadow` | 极淡暖灰阴影 | 卡片阴影（低不透明度，轻盈浮起） |
| `--radius` | `16px` | 卡片圆角 |

> 自由时间（gap）不再有填充块：`.tl-block.gap` 透明背景、无虚线框，仅用 `--text-3` 细灰文字标注，把负空间还给空闲。

**重要设计约束（别违反）：颜色只分三族彩色 + 中性底**——

- **青蓝 `--accent`** = 工作/行动：主按钮 `.btn.primary`、当前段 `.tl-row.is-now .tl-time`、now 卡片左边条、检测结果标题、地形图任务块。
- **青绿/薄荷 `--charge`/`--rest`/`--done`** = 恢复/休息：地形图「充电/休息」块、已完成勾选、挡位1 badge。
- **珊瑚 `--gear-3`/`--stuck-text`** = 紧迫/求救：只用于「冲刺」挡位 badge 和「我卡住了」按钮，别扩散。
- **暖米中性** = 其余一切（背景、边框、次级文字、pill/tag 底；自由时间 = 无块留白 + 细灰文字）。

挡位专属色：挡位1=青绿 `--gear-1`、挡位2=青蓝 `--gear-2`、挡位3=珊瑚 `--gear-3`，用于顶栏 badge、now 卡片挡位 chip、复盘柱图（`renderReview` 内联 `background:var(--gear-${g})`）。

地形图**分色**（卡片内有图例 `.legend`）：任务=青蓝 `--accent`、充电=青绿 `--charge`、休息=薄荷 `--rest`、自由=无块留白（细灰字 `--text-3`）、已过去=灰底 `--surface-2`（`.tl-block.past`）。`renderTimeline()` 已在 `.tl-block` 输出 `task/charge/rest/gap/past` class，对应样式在 `styles.css` 的 `.tl-block.*`。当前段 = 左 4px 深色竖条 + 圆点（`--accent`）+ 时间标签转青蓝。

**历史**：早前是「琥珀 `#C9821F` + 深绿 `#2e8f83`」方案（琥珀只准三处、地形图统一深绿不分色）。后改为「暖米 + 陶土 + 苔绿」体系，恢复地形图分色 + 图例。再后按「透亮 / 呼吸 / 轻盈」方向重构为「低饱和浅米白 + 焦糖 + 青薄荷」：底色更白、去灰调、8px 栅格、卡片纯白 + 极淡阴影、自由时间改无块留白、按钮统一为主色填充 / 描边两态。再后按用户反馈去掉全页橙色：主强调由焦糖改为**青蓝**（橙只留给「冲刺」挡位 + 「我卡住了」），新增三挡位专属色，并让排程只排「尚未过去」的时间（已过去时段灰显为「已过去」）、午间充电固定在 12:00（晚起不再误标「午间」）。再后按「当前空槽刚开始就该用」的反馈，把刚开始（未过半、15 分钟内）的当前 30 分钟槽位也纳入可排，快结束的才跳过（`buildDaySchedule` 里 `GRACE=15`）。

## 改布局 / 改结构

- **DOM 结构**（加/删/挪卡片、按钮、面板、弹窗）→ `index.html`。
- **视觉布局**（栅格、间距、圆角、阴影、响应式）→ `styles.css`：
  - `.layout`：左右两栏栅格 `1fr 380px`，断点 `@media (max-width:900px)` 变单栏。
  - `.card`、`.timeline`、`.tl-row`（时间列 52px + 块）、`.tabs/.panel`、`.modal`。
- **渲染内容**（哪个数据填进哪个卡片、时间轴怎么生成）→ `app.js`：
  - `renderNow()` 当下安排、`renderTimeline()` 地形图、`renderReview()` 复盘、`renderBadge()` 顶栏挡位、`renderTaskList()` 任务列表。
  - `buildDaySchedule()` 排程引擎、`orderPendingTasks()` 待排排序、`renderQueue()` 队列页、`detectGear()` 挡位检测、`importTasksJson()` 导入。

## 启动 & 数据

- 双击 `启动.bat` → `node server.js` → 自动打开 `http://localhost:8642`。
- 首次打开弹「早晨流程」，填起床时间 + 睡眠，生成当天安排。
- 任务来源：跑 `/decompose-tasks 你的md` → skill 写 `inbox/tasks.json` → 工具点「重新读取 inbox」或刷新导入（自动去重，手动任务保留）。
- 桌面快捷方式：`桌面\精力助手.lnk` 指向 `启动.bat`。
- 自动启动：Windows 计划任务「精力助手」每天 8:00 运行 `启动.bat`（会弹控制台窗口，属正常；8 点未开机则下次登录补跑）。

## 坑（重要，别再踩）

1. **`启动.bat` 必须是 CRLF 换行 + 纯 ASCII（不能有中文）**。Write 工具默认写 LF，会让 cmd 解析出错。改它要用 bash heredoc + `sed` 转 CRLF。
2. **本机 `.js` 关联的是 WScript.exe 不是 Node**，双击 `server.js` 会弹「打开方式」。启动入口只有 `启动.bat`。
3. 服务器绑定 `127.0.0.1:8642`，端口被占会 `listening` 事件里自动换随机端口。
4. 服务是静态文件服务器，每次请求现读文件——**改完 html/css/js 刷新浏览器即可**，无需重启服务。

## 已知问题（未修）

暂无。

> 顺延模型（设计，非 bug）：因为每天的起床/睡眠不同，**不做未来几天的预排**。今天装不下的任务进入「队列」页的「待排」区（只按优先级排队、不标时间）；每天早晨汇报当天起床时间后，用当天真实作息把未完成任务（含待排）排进「那一天」。
>
> 已修：检测完挡位 badge 不变的问题——`renderBadge()` 现在优先显示当天最近一次检测结果，没有检测记录才退回日程挡位。
